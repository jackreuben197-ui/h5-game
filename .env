VITE_API_BASE_URL=https://preview.trackyourchoice.com/api
# 主域名，渠道包泛域名解析需要用到，必须配置正确，否则渠道包相关功能无法使用。

# bridge 协议来源开关。vite.config.ts 据此切换 @bridge-protocol alias：
#   pokerqueen  → 本地 src/bridge/protocol/，对接旧 pokerqueen cocos 项目（默认）
#   h5-cc-game  → npm 包 @silenthill/h5-cc-bridge，与新 h5-cc-game cocos 项目共用同一份协议
VITE_BRIDGE_TARGET=pokerqueen
